import { CreatePeople } from "./peoples/module_example.js";

const ppls = [CreatePeople('beto'), CreatePeople('gaston')]

ppls.push(CreatePeople('hogo'), CreatePeople('jamersom'), CreatePeople('yuri'))